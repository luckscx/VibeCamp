# VibeCamp 知识框架 v0.2

> AI 构建训练营。目标用户：**零编程基础**，想用 AI 做出自己的小游戏 / 小应用。
> 一句话定位：**不是教你写代码，而是教你把话说清楚，让 AI 把东西做出来。**

---

## 一、核心判断（决定了整个框架的形状）

### 1. 小白真正的门槛不是"写代码"，是这四件事

| 门槛 | 表现 | 框架里的对应解法 |
|---|---|---|
| **说不清** | 脑子里的东西描述出来就变味，AI 做出来的不是想要的 | 需求模板 + "反向追问"提示词（让 AI 先问你 10 个问题） |
| **看不懂** | AI 说"已完成"，但不知道它改了什么、跑在哪 | 术语表 + 每步"你应该看到什么"验收点 |
| **不敢动** | 报错就慌，只会来回贴报错，越改越乱 | 排错决策树 + "止损失败三板斧"（回滚/Git/新开会话） |
| **跑不起来** | 本地能跑 ≠ 别人能打开 | 发布路径单独成章（预览 vs 部署） |

### 2. 关键洞察：会话不是文件，AI 会"失忆"
- CodeBuddy 会话存在本地，**不支持跨设备同步**
- 上下文会满，满了就压缩，压缩就有信息丢失
- → 所以必须教 **"外部记忆"**：`CODEBUDDY.md` 项目记忆 + `PROGRESS.md` 进度文件
- 这是小白最容易踩、又最少人讲的一个坑

### 3. 工具分工（决定"我该用哪个"）
- **WorkBuddy** = 中国版 Cowork，面向知识工作者，**类无代码体验** → 适合：自动化办公、文档/数据处理、不想碰命令行的人
- **CodeBuddy Code (CLI)** = 对标 Claude Code 的 Coding Agent → 适合：真的要做一个能跑的游戏/应用，愿意敲几行命令
- **CodeBuddy IDE 插件** = 在编辑器里用 → 适合：有一点基础，想边看代码边改

---

## 二、框架主干：一条主线 + 五个资源库

```
第 0 章  先搞清楚：AI 能帮你做到哪一步        ← 建立正确预期，防劝退
第 1 章  选工具：我该用哪一个                  ← 工具矩阵 + 决策树
第 2 章  装环境：从零到"AI 能干活"（最劝退的一章，要写细）
第 3 章  第一次对话：把想法说清楚              ← 核心章，需求模板 + 反向追问
第 4 章  让它跑起来：预览、看效果、提修改       ← 核心章，迭代循环
第 5 章  修 bug：报错了怎么办                  ← 排错决策树 + 止损三板斧
第 6 章  发出去：让别人也能打开                ← 发布路径
第 7 章  下一步：从"能跑"到"能用"             ← 进阶指引

资源库 A：项目模板（抄作业用）
资源库 B：提示词库（可复制粘贴）
资源库 C：排错手册（症状 → 原因 → 解法）
资源库 D：术语表（白话对照）
```

---

## 三、已核实的硬事实（写内容时只能基于这些，不许编）

### 安装与命令
- npm 包名：**`@tencent-ai/codebuddy-code`**（最新 2.141.0）
- 安装：`npm install -g @tencent-ai/codebuddy-code`
- 原生安装器（Beta，无需 Node）：`curl -fsSL https://copilot.tencent.com/cli/install.sh | bash`
- 要求：Node.js **≥ 18.20**（Windows 必须装 Git Bash）
- 更新：`codebuddy update`
- 验证：`codebuddy --version`

### 登录方式（4 选 1）
Chinese Site（国内站）/ International Site（国际站）/ Enterprise Domain（企业专享版）/ iOA（腾讯内部）

### 内置斜杠命令（20 个，已核实）
| 命令 | 用途 |
|---|---|
| `/init` | **初始化项目上下文（强烈推荐，省 30-50% token）** |
| `/help` | 帮助 |
| `/clear` | 清上下文，开新会话 |
| `/compact` | 压缩上下文 |
| `/model` | 切换模型 |
| `/memory` | 管理长期记忆 |
| `/mcp` | 管理 MCP 连接 |
| `/permissions` | 权限 |
| `/cost` | 看花费 |
| `/status` | 看状态 |
| `/doctor` | 环境自检 |
| `/config` | 配置（含语言设置） |
| `/agents` | 管理子代理 |
| `/add-dir` | 添加目录上下文 |
| `/bashes` | 后台任务 |
| `/terminal-setup` | Shift+Enter 换行 |
| `/login` `/logout` `/upgrade` | 登录/登出/升级 |

### 三个扩展机制（小白进阶的杠杆）
| 机制 | 放哪 | 干什么 |
|---|---|---|
| **自定义斜杠命令** | `.codebuddy/commands/`（项目）或 `~/.codebuddy/commands/`（全局） | 把常用提示词固化成 `/xxx`；子目录自动变 `/frontend:build` |
| **子代理 Sub-Agent** | `.codebuddy/agents/` 或 `~/.codebuddy/agents/` | 封装角色/流程，frontmatter 可配 `name/description/model/tools` |
| **Skills 技能** | Skills 目录，含 `SKILL.md` + frontmatter | 比命令更重，能带脚本、配 allowed-tools 白名单、Context Fork |

### 记忆机制（外部记忆 = 防失忆的关键）
- 用户级：`~/.codebuddy/CODEBUDDY.md`
- 项目级：`./CODEBUDDY.md`
- 追加到 `## CodeBuddy Added Memories` 章节，列表项形式
- 支持 `@import ./docs/xxx.md` 引用外部文件

### 会话管理
- `codebuddy --resume` / `--resume <session-id>` / `--continue`
- **会话仅存本地，不支持跨设备同步** ← 重点强调
- 跨设备协作靠：Git commit / PROGRESS.md / 让 AI 生成总结

### 常用工作流命令
- 无头模式：`codebuddy -p "提示词"`
- JSON 输出：`codebuddy -p "..." --output-format json > out.json`
- 管道：`git diff HEAD~1 | codebuddy -p "审查这次提交"`

### 已核实的官方文档结构（用于后续深挖）
入门指南 / 配置 / 高级功能（子代理、Agent Teams、Skills、Hooks、插件、检查点、Git Worktree、远程控制、Web UI、企业微信机器人、Daemon）/ 自动化（Goal、定时任务、Channels）/ 安全 / 参考文档 / SDK

---

## 四、还没拿到的材料（下一步要补）

| 缺口 | 重要性 | 怎么拿 |
|---|---|---|
| CodeBuddy Skills / Hooks / 检查点 的正文 | 高 | 文档站是 SPA，curl 拿不到；改用浏览器渲染抓 |
| WorkBuddy 上手流程（有没有 CLI / 网页端） | 高 | km.woa.com 文章（内网）+ 官方文档 workbuddy 分区 |
| 小白真实做小游戏的完整案例 | 高 | km 上"零编程经验者数天构建完整应用"类文章 |
| 部署/发布路径（Claw / 静态托管） | 中 | 复用已有 claw-server-deployment skill 经验 |
| 常见报错的真实样本 | 中 | 自己跑一遍，记录真实报错 |

---

## 五、内容写作原则（约束后面所有产出）

1. **不编 API**。每个命令、路径、参数都必须在上表能找到出处。拿不准就标"待验证"。
2. **每步都有验收点**。写"你应该看到 X"，而不是"然后就成功了"。
3. **中文优先**，术语第一次出现给白话解释。
4. **反面案例要真**。报错信息要贴真实的，不要编造。
5. **零门槛不等于零思考**。明确告诉用户哪些地方必须自己决策（AI 替不了）。
